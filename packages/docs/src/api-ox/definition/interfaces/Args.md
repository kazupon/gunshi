# Args

An object that contains [argument schema](/api-ox/definition/interfaces/ArgSchema.md).

This type is used to define the structure and validation rules for command line arguments.

## Signature

```ts
interface Args
```
