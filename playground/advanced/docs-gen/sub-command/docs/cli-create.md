# Create Command

```sh
My CLI tool (my-cli v1.0.0)
Create a new resource

USAGE:
  my-cli create <OPTIONS>

OPTIONS:
  -h, --help                 Display this help message
  -v, --version              Display this version
  -n, --name <name>          Name of the resource

```