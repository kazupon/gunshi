mycli (mycli v1.0.0)

USAGE:
  mycli [COMMANDS] <OPTIONS>

COMMANDS:
  [entry] <OPTIONS>
  server <OPTIONS>         Start the development server

For more info, run any command with the `--help` flag:
  mycli --help
  mycli server --help

OPTIONS:
  -h, --help             Display this help message
  -v, --version          Display this version

